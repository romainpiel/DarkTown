language: android
android:
  components:
    - platform-tools
    - tools
    - build-tools-21.0.2
    - android-18
    - android-21
    - extra-android-m2repository
    - extra-android-support

notifications:
  email: false

before_install:

  # for gradle output style
  - export TERM=dumb

  # environment info
  - ./gradlew -v
  - uname -a

# Let's try to build...
script: ./gradlew testDebug -i